# 今日清单 Todo List

一个零依赖、响应式的 Todo List 单页应用。任务保存在浏览器的 `localStorage` 中。

## 功能

- 新增、完成、编辑和删除任务
- 全部 / 待完成 / 已完成筛选
- 一键清除已完成任务
- 完成进度与任务统计
- 本地持久化、移动端适配和键盘操作支持

## 本地运行

使用任意静态文件服务器打开项目目录，例如：

```bash
python -m http.server 8000
```

然后访问 `http://localhost:8000`。

## 测试

```bash
npm test
```
